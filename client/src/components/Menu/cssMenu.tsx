import styled, { css } from "styled-components";

const StyleTotal = styled.div`
z-index: 10;
  .sider {
    &::-webkit-scrollbar {
      display: none;
    }
    .ant-menu-item {
      /* color: red; */
    }
  }
`;

export default StyleTotal;
